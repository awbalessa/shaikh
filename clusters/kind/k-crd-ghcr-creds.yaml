apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ghcr-creds
  namespace: flux-system
spec:
  interval: 10m
  prune: true
  path: ./clusters/kind
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
